{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "WatcherDecision",
  "type": "object",
  "additionalProperties": false,
  "required": ["action", "value"],
  "properties": {
    "action": {
      "type": "string",
      "enum": ["text", "key", "enter", "skip"],
      "description": "What to do in the Claude pane."
    },
    "value": {
      "type": ["string", "null"],
      "description": "For action=text: the reply text. For action=key: a single digit '1'-'9'. For action=skip: a short reason. null for action=enter."
    }
  }
}
